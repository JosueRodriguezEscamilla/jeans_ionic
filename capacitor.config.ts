import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'APD_Ionic_Jeans',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
